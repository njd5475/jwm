ICONDIR = $(DESTDIR)@DATADIR@/nwm

ICONS = nwm.xbm nwm-gray.svg nwm-blue.svg nwm-orange.svg

install:
	install -d $(ICONDIR)
	for f in $(ICONS) ; do \
		install -m 0644 $$f $(ICONDIR)/$$f ; \
	done

uninstall:
	for f in $(ICONS) ; do \
		rm -f $(ICONDIR)/$$f ; \
	done
